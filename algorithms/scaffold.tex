\begin{algorithm}[ht]
% \SetAlgoNoLine
% \DontPrintSemicolon
\SetKwInOut{Input}{Input}
\Input{server learning rate $\eta_g,$ client learning rates $\eta_k, k=1, \ldots K$}
{\bfseries Initiation:} global (server) model parameters $\theta^{(0)} \in \R^d,$ control variates $c^{(0)}, c_k^{(0)} \in \R^d,$ $k = 1, \ldots, K$ \;
\For{each round $t = 0, 1, \cdots, T-1$}{
    $S^{(t)} \gets$ (random set of clients) $\subseteq [K]$\;
    broadcast control variates $(\theta^{(t)}, c^{(t)})$ to clients $k \in S^{(t)}$\;
    \For{each client $k \in \mathcal{S}^{(t)}$ {\bfseries in parallel}}{
        xx\;
    }
}
\caption{\texttt{SCAFFOLD}算法\cite{karimireddy2020scaffold}的伪代码}
\label{algo:scaffold}
\end{algorithm}
